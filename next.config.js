/** @type {import('next').NextConfig} */
const nextConfig = {
  // Static export — works on Vercel AND as plain static files
  // Remove this line if you later add API routes / server actions
  // output: 'export',
}
module.exports = nextConfig
